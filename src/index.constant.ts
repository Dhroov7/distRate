export const MAX_BACKOFF_DELAY = 5000; // maximum delay between retries in milliseconds
export const INITIAL_BACKOFF_DELAY = 50; // initial delay between retries in milliseconds
export const BACKOFF_MULTIPLIER = 1.5; // multiplier for increasing delay between retries